#!/usr/bin/env groovy
pipeline {
    agent any

    stages {
        stage("Custom pre stage") {
            echo "CUSTOM PRE STAGE !!!"
        }

        stage("Custom post stage") {
            echo "CUSTOM POST STAGE !!!"
        }
    }
}